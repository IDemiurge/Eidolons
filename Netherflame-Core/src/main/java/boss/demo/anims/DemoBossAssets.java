package boss.demo.anims;

import boss.anims.BossAssets;
import boss.BossManager;
import boss.demo.DemoBoss;

public class DemoBossAssets extends BossAssets<DemoBoss> {
    public DemoBossAssets(BossManager manager) {
        super(manager);
    }

    @Override
    public void init() {
        /*
        lite mode, or some smarter system?
        wait till loaded - how?
        lazy mode?
        ktx mode
        not just boss parts?

         */


    }
}
