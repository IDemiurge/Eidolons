package main.test.auto;

import main.content.OBJ_TYPES;
import main.entity.obj.DC_HeroObj;
import main.entity.obj.DC_WeaponObj;

public class Constraint {

    public void init(DC_HeroObj unit) {
//        if (e.getOBJ_TYPE_ENUM() == OBJ_TYPES.SKILLS) {
//            if (type == WEAPON)
//                unit.setWeapon(new DC_WeaponObj(getWeaponTypeForGroup(e.getGroupingKey()), unit));
//
//        }
    }

}
