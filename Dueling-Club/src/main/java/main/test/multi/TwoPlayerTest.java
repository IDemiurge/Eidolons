package main.test.multi;

import main.client.DC_Engine;

/**
 * request host/client input or Create game (specs) => Join game (game list
 * panel? Lobby?)
 * <p>
 * get game data => player
 *
 * @author Regulus
 */
public class TwoPlayerTest {

    public static void main(String[] args) {
        DC_Engine.init();

        //lauchGame(data);
    }

    private static void initDC_Game() {
/*
        DC_GameData data = new DC_GameData(gameName, objData, objData2,
				player1, player2);
		game = new DC_Game(data);
		initHeroes();
		game.initComponents();*/
    }

    public boolean joinGame() {
//		launchGame(hostData, handler);
        return false;
    }
}
