package main.system.ai.logic.companion;

import main.system.ai.UnitAI;
import main.system.ai.logic.actions.Action;
import main.system.ai.logic.actions.ActionSequence;

public class SpecialPriorities {

    public static void applySpecialPriorities(ActionSequence sequence) {
        UnitAI ai = sequence.getAi();
        int priority = sequence.getPriority();
        for (Action a : sequence.getActions()) {

        }

        //checkNullify

    }
}
