package main.rules.mechanics;

import main.entity.obj.top.DC_ActiveObj;

public class TerrainRule {

    public static void addMoveCost(DC_ActiveObj activeObj) {
        // getOrCreate move direction for std moves
        // special moves can only getOrCreate post-penalty in sta?

        // special penalty mechanic - reduce ap if possible, sta if not, foc if
        // not or TODO USE MOVEMENT MGR

    }

}
